# Kopieren nach .env.local — nur nötig, wenn die API nicht auf dem Standard für lokales dotnet run läuft:
# NEXT_PUBLIC_API_URL=http://localhost:5112
#
# Nur API aus Docker (Port 8080), Web mit npm run dev:
# NEXT_PUBLIC_API_URL=http://localhost:8080
